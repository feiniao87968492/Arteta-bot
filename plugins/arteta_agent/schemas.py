"""Shared schema placeholders for the agent package.

The current registry keeps JSON schemas directly in each ToolSpec. This module
is reserved for shared schema helpers as the tool surface grows.
"""
